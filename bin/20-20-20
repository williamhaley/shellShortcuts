#!/usr/bin/env bash

(
  while true;
  do
    sleep 1800 # 20 * 60 

    if [ "${IS_MAC}" = "true" ];
    then
      osascript -e 'display dialog "Rest your eyes"'
    fi

    if [ "${IS_LINUX}" = "true" ];
    then
      notify-send --expire-time=20000 "Rest your eyes"
    fi
  done
) &

