#!/usr/bin/env bash

# Use -d as a portable flag across Unix/Linux
dir=$(mktemp -d)

ffmpeg \
	-i $1 \
	-vf scale=320:-1:flags=lanczos,fps=10 \
	$dir/ffout%03d.png

convert -loop 0 $dir/ffout*.png output.gif

