#!/usr/bin/env bash

year=$1
month=01
day=01

if [ -z "${year}" ];
then
    echo "pass in a four digit year as the only argument"
    exit 1
fi

for file in *;
do
  exiftool "-datetimeoriginal=${year}:${month}:${day} 12:00:00" "${file}"
  touch -t ${year}${month}${day}1200 "${file}"
done

