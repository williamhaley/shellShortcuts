#!/usr/bin/env bash

CONTAINERS=$(docker ps -a --format "{{.ID}}")

for C in "$CONTAINERS"
do
	docker rm $C
done

IMAGES=$(docker images --format "{{.ID}}")

for I in "$IMAGES"
do
	docker rmi $I
done
