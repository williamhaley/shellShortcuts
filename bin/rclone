#!/usr/bin/env bash

# Wrapper around rclone. Add backup-dir by default.
# If you want the potentially destructive version
# of rclone, call wrclone directly.

wrclone \
	--backup-dir "${HOME}/rclone-backup/$(date +%s)" \
	"$@"

